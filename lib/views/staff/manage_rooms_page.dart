import 'package:flutter/material.dart';

class ManageRoomsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manage Rooms')),
      body: Center(child: Text('Manage rooms functionality will be here')),
    );
  }
}
