import 'package:flutter/material.dart';

class ApproveRequestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Approve Requests')),
      body: Center(child: Text('Approve requests functionality will be here')),
    );
  }
}
