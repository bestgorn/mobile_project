import 'package:flutter/material.dart';

class ApproverDashboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Approver Dashboard')),
      body: Center(child: Text('Approver dashboard functionality will be here')),
    );
  }
}
