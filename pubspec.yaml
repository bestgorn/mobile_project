name: mobile_mid
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0" # ระบุช่วง SDK ที่ Flutter รองรับ

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2 # แพ็กเกจ http สำหรับเรียก API
  socket_io_client: ^2.0.0 # แพ็กเกจ socket.io client
  shared_preferences: ^2.0.0 # เพิ่มแพ็กเกจ SharedPreferences

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
